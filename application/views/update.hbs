<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="UTF-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	{{> css}}
	<title>Document</title>
</head>

<body>
	{{> navbar}}
	<div class="flex flex-col max-w-4xl mx-auto mt-10 px-5">
		{{!-- --}}
		{{#if message}}
		<div class="capitalize text-xs text-red-500 text-left my-5 w-full">
			{{message}}
		</div>
		{{!-- --}}
		{{/if}}
		{{!-- --}}
		<form action="/update/{{image.id}}" method="POST">
			<div class="flex flex-col space-y-5 my-2">
				<p>
					Please enter your image name.
				</p>
				<input name="name" class="outline-none px-5 py-2 bg-gray-200 text-gray-700" type="text"
					placeholder="Image name" />
			</div>
			<button class="bg-gray-100 px-5 py-2 rounded-sm hover:bg-gray-200 transition-colors duration-200 my-2"
				type="submit">Update</button>
		</form>
		<img class="w-96 h-full object-cover" src='../{{image.path}}' alt="">
		{{!-- --}}
	</div>
</body>

</html>